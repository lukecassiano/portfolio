---
phase: 03-sandbar-case-study-stubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sandbar/PipelineDiagram.tsx
autonomous: true
requirements: [SAND-03]
must_haves:
  truths:
    - "A recruiter sees a crisp, non-pixelated pipeline diagram at every viewport width"
    - "A screen reader announces an accessible name for the diagram"
    - "The diagram shows 4 stages flowing left to right: Buoy Data, NOAA Grid, Ensemble Model, Surf Forecast"
  artifacts:
    - path: "components/sandbar/PipelineDiagram.tsx"
      provides: "Inline SVG React component (Server Component) for the Sandbar forecast pipeline"
      exports: ["PipelineDiagram"]
      min_lines: 30
  key_links:
    - from: "components/sandbar/PipelineDiagram.tsx"
      to: "svg <title id=\"pipeline-title\">"
      via: "aria-labelledby on <svg>"
      pattern: "aria-labelledby=\"pipeline-title\""
---

<objective>
Build the `PipelineDiagram` component: an inline SVG React component (Server Component, no 'use client') that renders the 4-stage Sandbar forecast pipeline. This is the only net-new component in Phase 3 and is consumed by the /sandbar case study page in Wave 2.

Purpose: SAND-03 requires the pipeline rendered as inline SVG (not an `<img>`, not SVGR) so it stays crisp at all widths and carries proper accessibility markup.
Output: `components/sandbar/PipelineDiagram.tsx` exporting `PipelineDiagram`.
</objective>

<execution_context>
@C:/Users/lukec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sandbar-case-study-stubs/03-UI-SPEC.md
@.planning/phases/03-sandbar-case-study-stubs/03-RESEARCH.md

<interfaces>
<!-- This component is a leaf — it imports nothing from the codebase. -->
<!-- It is consumed by app/sandbar/page.tsx (Wave 2, plan 03-03) as: -->
<!--   import { PipelineDiagram } from '@/components/sandbar/PipelineDiagram' -->
<!--   <PipelineDiagram /> -->
<!-- It must be a named export (NOT default) to match this import. -->
<!-- Font in SVG <text> must reference the existing CSS var: fontFamily="var(--font-mono)" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PipelineDiagram inline SVG component</name>
  <files>components/sandbar/PipelineDiagram.tsx</files>
  <read_first>
    - .planning/phases/03-sandbar-case-study-stubs/03-UI-SPEC.md (lines 254-287: pipeline diagram contract; lines 296-308: exact copy strings)
    - .planning/phases/03-sandbar-case-study-stubs/03-RESEARCH.md (lines 409-458: minimal valid PipelineDiagram SVG structure)
    - components/scenes/SandbarScene.tsx (confirms font-mono usage pattern and ink color #1A1A1A conventions in this project)
  </read_first>
  <action>
    Create the file `components/sandbar/PipelineDiagram.tsx` (create the `components/sandbar/` directory). It is a Server Component — do NOT add 'use client'. It imports nothing.

    Export a named function: `export function PipelineDiagram()`.

    The component returns a `<figure className="my-8">` containing an inline `<svg>` followed by a `<figcaption>`.

    The `<svg>` element MUST have exactly these attributes:
    - `viewBox="0 0 800 200"`
    - `width="100%"`
    - `role="img"`
    - `aria-labelledby="pipeline-title"`
    - `aria-label="Data pipeline diagram showing the flow from buoy data through NOAA grid to ensemble model to surf forecast output"`
    - Do NOT set a fixed pixel `width` or `height` attribute (only `width="100%"`).

    The FIRST child of `<svg>` MUST be:
    `<title id="pipeline-title">Sandbar data pipeline: buoy data to NOAA grid to ensemble model to surf forecast</title>`
    (the title must come before any rect/line/text — required for screen readers).

    Render 4 stage rectangles in a horizontal row connected by 3 arrows, using these exact coordinates and labels (the Ensemble Model node is emphasized with a larger box and thicker stroke):

    - Stage 1 "Buoy Data": `<rect x="20" y="70" width="140" height="60" rx="4" fill="none" stroke="#1A1A1A" strokeWidth="1.5" />` and `<text x="90" y="105" textAnchor="middle" fontFamily="var(--font-mono)" fontSize="12" fill="#1A1A1A">Buoy Data</text>`
    - Arrow 1→2: `<line x1="160" y1="100" x2="200" y2="100" stroke="#1A1A1A" strokeWidth="1.5" markerEnd="url(#arrow)" />`
    - Stage 2 "NOAA Grid": `<rect x="200" y="70" width="140" height="60" rx="4" fill="none" stroke="#1A1A1A" strokeWidth="1.5" />` and `<text x="270" y="105" textAnchor="middle" fontFamily="var(--font-mono)" fontSize="12" fill="#1A1A1A">NOAA Grid</text>`
    - Arrow 2→3: `<line x1="340" y1="100" x2="380" y2="100" stroke="#1A1A1A" strokeWidth="1.5" markerEnd="url(#arrow)" />`
    - Stage 3 "Ensemble Model" (emphasized): `<rect x="380" y="60" width="160" height="80" rx="4" fill="none" stroke="#1A1A1A" strokeWidth="2" />` and `<text x="460" y="105" textAnchor="middle" fontFamily="var(--font-mono)" fontSize="12" fill="#1A1A1A">Ensemble Model</text>`
    - Arrow 3→4: `<line x1="540" y1="100" x2="580" y2="100" stroke="#1A1A1A" strokeWidth="1.5" markerEnd="url(#arrow)" />`
    - Stage 4 "Surf Forecast": `<rect x="580" y="70" width="160" height="60" rx="4" fill="none" stroke="#1A1A1A" strokeWidth="1.5" />` and `<text x="660" y="105" textAnchor="middle" fontFamily="var(--font-mono)" fontSize="12" fill="#1A1A1A">Surf Forecast</text>`

    Add the arrow marker definition inside a `<defs>` block:
    `<marker id="arrow" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0 L0,6 L8,3 z" fill="#1A1A1A" /></marker>`

    After the `</svg>`, add the caption:
    `<figcaption className="font-mono font-medium text-xs tracking-[0.08em] mt-4 text-center">BUOY DATA → NOAA GRID → ENSEMBLE MODEL → SURF FORECAST</figcaption>`

    Note on JSX: SVG attributes in React use camelCase (`strokeWidth`, `textAnchor`, `markerEnd`, `fontFamily`, `fontSize`, `markerWidth`, `refX`, `refY`). The arrow `→` and the caption text are literal Unicode characters — do not use HTML entities.
  </action>
  <verify>
    <automated>npx next lint --file components/sandbar/PipelineDiagram.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `components/sandbar/PipelineDiagram.tsx` exists
    - `grep -c "use client" components/sandbar/PipelineDiagram.tsx` returns 0 (no client directive)
    - `grep "export function PipelineDiagram" components/sandbar/PipelineDiagram.tsx` matches (named export, not default)
    - `grep 'role="img"' components/sandbar/PipelineDiagram.tsx` matches
    - `grep 'aria-labelledby="pipeline-title"' components/sandbar/PipelineDiagram.tsx` matches
    - `grep 'id="pipeline-title"' components/sandbar/PipelineDiagram.tsx` matches AND the `<title` element appears before the first `<rect` element in file order
    - `grep 'width="100%"' components/sandbar/PipelineDiagram.tsx` matches
    - `grep 'viewBox="0 0 800 200"' components/sandbar/PipelineDiagram.tsx` matches
    - File contains all four labels: `Buoy Data`, `NOAA Grid`, `Ensemble Model`, `Surf Forecast`
    - `grep 'var(--font-mono)' components/sandbar/PipelineDiagram.tsx` matches (font references project CSS var)
    - `grep 'BUOY DATA → NOAA GRID → ENSEMBLE MODEL → SURF FORECAST' components/sandbar/PipelineDiagram.tsx` matches (figcaption present)
    - File contains exactly 3 `<line` arrow elements and `markerEnd="url(#arrow)"` appears 3 times
    - File does NOT contain an `<img` tag
  </acceptance_criteria>
  <done>PipelineDiagram.tsx exists as a Server Component with a single inline SVG (title-first, role="img", aria-labelledby, width="100%", viewBox), 4 labeled stages, 3 arrows, and the figcaption; next lint passes for the file.</done>
</task>

</tasks>

<verification>
- `npx next lint --file components/sandbar/PipelineDiagram.tsx` passes with no errors
- The component is a Server Component (no 'use client')
- SVG accessibility markup is complete: title-first, role="img", aria-labelledby
- All 4 stage labels and the figcaption text match the UI-SPEC copy contract exactly
</verification>

<success_criteria>
- `components/sandbar/PipelineDiagram.tsx` exists and named-exports `PipelineDiagram`
- The diagram is inline SVG (no `<img>`), responsive via `width="100%"` + `viewBox`
- Accessibility: `<title id="pipeline-title">` is the first SVG child, `aria-labelledby` and `role="img"` present
- Ready to be imported by `app/sandbar/page.tsx` in Wave 2
</success_criteria>

<output>
After completion, create `.planning/phases/03-sandbar-case-study-stubs/03-01-SUMMARY.md`
</output>
