---
phase: 03-sandbar-case-study-stubs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/belief-agent/page.tsx
  - app/whitehelmet/page.tsx
autonomous: true
requirements: [STUB-01, STUB-02]
must_haves:
  truths:
    - "Visiting /belief-agent loads a full-viewport hero scene (not a 404) with the indigo→green blob, 'Belief Agent' wordmark, and an honest in-progress message"
    - "Visiting /whitehelmet loads a full-viewport hero scene (not a 404) with the ochre→dust blob, 'WhiteHelmet' wordmark, and an honest in-progress message"
    - "A keyboard user can use '← Back' on either stub to return to the landing page"
  artifacts:
    - path: "app/belief-agent/page.tsx"
      provides: "Belief Agent stub route — Server Component full-viewport scene"
      contains: "main-content"
      min_lines: 25
    - path: "app/whitehelmet/page.tsx"
      provides: "WhiteHelmet stub route — Server Component full-viewport scene"
      contains: "main-content"
      min_lines: 25
  key_links:
    - from: "app/belief-agent/page.tsx"
      to: "/"
      via: "back nav anchor href"
      pattern: "href=\"/\""
    - from: "app/whitehelmet/page.tsx"
      to: "/"
      via: "back nav anchor href"
      pattern: "href=\"/\""
---

<objective>
Create the two stub route pages `/belief-agent` and `/whitehelmet`. Each is a standalone full-viewport hero scene that reuses the existing GradientBlob and SceneFadeIn components and adds back navigation plus an honest "case study in progress" message. These replace the current 404 responses for these routes.

Purpose: STUB-01 and STUB-02 — the WhiteHelmet landing scene CTA already links to /whitehelmet (currently 404); these pages make both routes resolve honestly.
Output: `app/belief-agent/page.tsx` and `app/whitehelmet/page.tsx` (both Server Components).
</objective>

<execution_context>
@C:/Users/lukec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sandbar-case-study-stubs/03-UI-SPEC.md
@.planning/phases/03-sandbar-case-study-stubs/03-RESEARCH.md

<interfaces>
<!-- Reused components (already implemented, confirmed in codebase). Import and use directly — do NOT re-implement. -->

From components/ui/GradientBlob.tsx (Client Component — but page stays Server because the client boundary lives inside the component):
```typescript
interface GradientBlobProps {
  colors: string[]
  size?: string                 // default '60vmax'
  position?: React.CSSProperties
  blur?: string                 // default '80px'
  className?: string
}
export function GradientBlob(props: GradientBlobProps)
// Already renders aria-hidden="true" and pointer-events-none. Do not add aria-hidden yourself.
```

From components/ui/SceneFadeIn.tsx (Client Component):
```typescript
interface SceneFadeInProps { children: React.ReactNode; className?: string }
export function SceneFadeIn(props: SceneFadeInProps)
// Already handles useReducedMotion. initial {opacity:0,y:12} -> whileInView {opacity:1,y:0}.
```

Reference: components/scenes/BeliefAgentScene.tsx and components/scenes/WhiteHelmetScene.tsx are the
exact visual templates. The stub pages copy their section className, blob props, and SceneFadeIn z-10
pattern, but: (a) wrap in <main id="main-content">, (b) add a back-nav anchor, (c) swap the tagline/stat
content for the honest in-progress message, and (d) REMOVE the SceneLink CTA (no case study to link to).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /belief-agent stub page</name>
  <files>app/belief-agent/page.tsx</files>
  <read_first>
    - components/scenes/BeliefAgentScene.tsx (exact section className, blob props, SceneFadeIn z-10 pattern to replicate)
    - .planning/phases/03-sandbar-case-study-stubs/03-UI-SPEC.md (lines 167-185: belief-agent layout; lines 312-320: copy; lines 124-132: blob spec; lines 210-228: back nav)
    - app/not-found.tsx (confirms `<main id="main-content">` pattern used on standalone route pages)
  </read_first>
  <action>
    Create `app/belief-agent/page.tsx` (create the `app/belief-agent/` directory). Server Component — do NOT add 'use client'.

    Imports:
    `import { GradientBlob } from '@/components/ui/GradientBlob'`
    `import { SceneFadeIn } from '@/components/ui/SceneFadeIn'`

    Export default function `BeliefAgentPage()`.

    Structure:
    ```
    <main id="main-content">
      <section
        aria-label="Belief Agent"
        className="relative min-h-[100svh] overflow-hidden flex flex-col items-center justify-center px-8 py-12 lg:px-16 lg:py-16 bg-[#0D0D12] text-cream"
      >
        <a
          href="/"
          className="absolute top-8 left-8 lg:top-12 lg:left-12 font-mono font-medium text-xs tracking-[0.08em] uppercase hover:opacity-60 transition-opacity duration-200 z-10"
        >
          ← Back
        </a>
        <GradientBlob
          colors={['#312E81', '#4ADE80']}
          size="65vmax"
          position={{ top: '5%', right: '-10%' }}
          blur="120px"
        />
        <SceneFadeIn className="relative z-10 flex flex-col items-center gap-6 text-center">
          <h1 className="wordmark" style={{ fontSize: 'clamp(36px, 6vw, 72px)', lineHeight: 1.1 }}>
            Belief Agent
          </h1>
          <p className="font-mono font-medium text-base leading-relaxed">
            This case study is in progress.
          </p>
          <p className="font-mono font-medium text-base leading-relaxed">
            The work is real — the write-up is catching up.
          </p>
          <p className="font-mono font-medium text-xs uppercase tracking-[0.08em]">
            CASE STUDY IN PROGRESS
          </p>
        </SceneFadeIn>
      </section>
    </main>
    ```

    Notes:
    - The wordmark here is `<h1>` (this is the page's single top-level heading), NOT `<h2>` as in the landing scene.
    - Use `text-cream` (Tailwind alias, confirmed) for the section text color — matches BeliefAgentScene.tsx exactly.
    - Do NOT import or render SceneLink. There is no forward CTA on a stub.
    - The `← Back` arrow is a literal Unicode `←` character, and the `—` in the message is a literal em dash. Do not use HTML entities.
  </action>
  <verify>
    <automated>npx next lint --file app/belief-agent/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `app/belief-agent/page.tsx` exists
    - `grep -c "use client" app/belief-agent/page.tsx` returns 0
    - `grep 'id="main-content"' app/belief-agent/page.tsx` matches
    - `grep 'min-h-\[100svh\]' app/belief-agent/page.tsx` matches (NOT min-h-screen)
    - `grep 'bg-\[#0D0D12\]' app/belief-agent/page.tsx` matches AND `grep 'text-cream' app/belief-agent/page.tsx` matches
    - `grep "'#312E81', '#4ADE80'" app/belief-agent/page.tsx` matches (blob colors)
    - `grep 'size="65vmax"' app/belief-agent/page.tsx` matches
    - `grep "top: '5%', right: '-10%'" app/belief-agent/page.tsx` matches
    - `grep 'href="/"' app/belief-agent/page.tsx` matches (back nav)
    - `grep 'absolute top-8 left-8' app/belief-agent/page.tsx` matches
    - File contains the strings `Belief Agent`, `This case study is in progress.`, `The work is real`, and `CASE STUDY IN PROGRESS`
    - `grep -c "SceneLink" app/belief-agent/page.tsx` returns 0 (no forward CTA)
    - File contains exactly one `<h1` element and zero `<h2` elements
  </acceptance_criteria>
  <done>/belief-agent resolves to a Server Component full-viewport scene with the indigo→green blob, single h1 wordmark, honest message, status label, and absolute top-left back nav; next lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create /whitehelmet stub page</name>
  <files>app/whitehelmet/page.tsx</files>
  <read_first>
    - components/scenes/WhiteHelmetScene.tsx (exact section className, blob props, SceneFadeIn z-10 pattern to replicate)
    - .planning/phases/03-sandbar-case-study-stubs/03-UI-SPEC.md (lines 187-204: whitehelmet layout; lines 322-330: copy; lines 124-132: blob spec)
    - app/belief-agent/page.tsx (the sibling stub created in Task 1 — replicate its structure, swapping background/blob/copy)
  </read_first>
  <action>
    Create `app/whitehelmet/page.tsx` (create the `app/whitehelmet/` directory). Server Component — do NOT add 'use client'.

    Imports:
    `import { GradientBlob } from '@/components/ui/GradientBlob'`
    `import { SceneFadeIn } from '@/components/ui/SceneFadeIn'`

    Export default function `WhiteHelmetPage()`.

    Structure (cream background, ink text — note NO bg/text override classes on the section, matching WhiteHelmetScene.tsx which inherits the cream/ink defaults):
    ```
    <main id="main-content">
      <section
        aria-label="WhiteHelmet"
        className="relative min-h-[100svh] overflow-hidden flex flex-col items-center justify-center px-8 py-12 lg:px-16 lg:py-16"
      >
        <a
          href="/"
          className="absolute top-8 left-8 lg:top-12 lg:left-12 font-mono font-medium text-xs tracking-[0.08em] uppercase hover:opacity-60 transition-opacity duration-200 z-10"
        >
          ← Back
        </a>
        <GradientBlob
          colors={['#D97706', '#A8A29E']}
          size="60vmax"
          position={{ bottom: '-15%', left: '50%', transform: 'translateX(-50%)' }}
          blur="80px"
        />
        <SceneFadeIn className="relative z-10 flex flex-col items-center gap-6 text-center">
          <h1 className="wordmark" style={{ fontSize: 'clamp(36px, 6vw, 72px)', lineHeight: 1.1 }}>
            WhiteHelmet
          </h1>
          <p className="font-mono font-medium text-base leading-relaxed">
            This case study is in progress.
          </p>
          <p className="font-mono font-medium text-base leading-relaxed">
            The work is real — documentation coming soon.
          </p>
          <p className="font-mono font-medium text-xs uppercase tracking-[0.08em]">
            CASE STUDY IN PROGRESS
          </p>
        </SceneFadeIn>
      </section>
    </main>
    ```

    Notes:
    - The wordmark is `<h1>` (single top-level heading), NOT `<h2>`.
    - Do NOT add `bg-` or `text-` override classes on the section — the cream background and ink text come from the global default (matches WhiteHelmetScene.tsx exactly).
    - The blob position uses `transform: 'translateX(-50%)'` inline (do NOT add Tailwind -translate-x-1/2 — avoids the double-transform pitfall noted in project decisions).
    - Do NOT import or render SceneLink.
    - Message line 2 differs from /belief-agent: "documentation coming soon." (not "the write-up is catching up.").
    - `←` and `—` are literal Unicode characters, not HTML entities.
  </action>
  <verify>
    <automated>npx next lint --file app/whitehelmet/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `app/whitehelmet/page.tsx` exists
    - `grep -c "use client" app/whitehelmet/page.tsx` returns 0
    - `grep 'id="main-content"' app/whitehelmet/page.tsx` matches
    - `grep 'min-h-\[100svh\]' app/whitehelmet/page.tsx` matches (NOT min-h-screen)
    - `grep "'#D97706', '#A8A29E'" app/whitehelmet/page.tsx` matches (blob colors)
    - `grep 'size="60vmax"' app/whitehelmet/page.tsx` matches
    - `grep "bottom: '-15%'" app/whitehelmet/page.tsx` matches AND `grep "transform: 'translateX(-50%)'" app/whitehelmet/page.tsx` matches
    - `grep 'href="/"' app/whitehelmet/page.tsx` matches (back nav)
    - `grep 'absolute top-8 left-8' app/whitehelmet/page.tsx` matches
    - File contains the strings `WhiteHelmet`, `This case study is in progress.`, `documentation coming soon.`, and `CASE STUDY IN PROGRESS`
    - `grep -c "bg-\[#" app/whitehelmet/page.tsx` returns 0 (no background override — inherits cream)
    - `grep -c "SceneLink" app/whitehelmet/page.tsx` returns 0
    - File contains exactly one `<h1` element and zero `<h2` elements
  </acceptance_criteria>
  <done>/whitehelmet resolves to a Server Component full-viewport scene on cream with the ochre→dust bottom-centered blob, single h1 wordmark, honest message, status label, and absolute top-left back nav; next lint passes.</done>
</task>

</tasks>

<verification>
- `npx next lint --file app/belief-agent/page.tsx` and `npx next lint --file app/whitehelmet/page.tsx` both pass
- Both pages are Server Components (no 'use client')
- Both wrap output in `<main id="main-content">` (skip-link target preserved)
- Blob props match the UI-SPEC blob table exactly for each page
- Neither page imports SceneLink (no forward CTA on stubs)
</verification>

<success_criteria>
- `/belief-agent` and `/whitehelmet` each render a full-viewport hero with blob, h1 wordmark, honest in-progress message, status label, and back navigation — neither returns a 404
- Each page has exactly one h1 and a valid heading hierarchy
- Back navigation anchor (`href="/"`) is keyboard-reachable and present in the DOM
</success_criteria>

<output>
After completion, create `.planning/phases/03-sandbar-case-study-stubs/03-02-SUMMARY.md`
</output>
