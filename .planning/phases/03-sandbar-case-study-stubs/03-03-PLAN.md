---
phase: 03-sandbar-case-study-stubs
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/sandbar/page.tsx
autonomous: true
requirements: [SAND-01, SAND-02, SAND-04, SAND-05, SAND-06]
must_haves:
  truths:
    - "Visiting /sandbar loads all five sections in order: Problem, Insight, Product, Under the Hood, What's Next"
    - "Prose is constrained to a max-w-2xl (~65ch) column on all breakpoints with no horizontal scroll"
    - "The page h1 and all section h2 headings render in Fraunces; all prose/captions render in IBM Plex Mono"
    - "A large Fraunces '87%' hero stat with a FORECAST ACCURACY label appears below the h1"
    - "The pipeline diagram appears inside the Product section"
    - "A '← Back' link at the top of the column returns the visitor to the landing page"
  artifacts:
    - path: "app/sandbar/page.tsx"
      provides: "Sandbar editorial case study page — Server Component, 5 sections + PipelineDiagram + Footer"
      contains: "main-content"
      min_lines: 80
  key_links:
    - from: "app/sandbar/page.tsx"
      to: "components/sandbar/PipelineDiagram.tsx"
      via: "import + <PipelineDiagram /> render in Product section"
      pattern: "PipelineDiagram"
    - from: "app/sandbar/page.tsx"
      to: "/"
      via: "back nav anchor href"
      pattern: "href=\"/\""
    - from: "app/sandbar/page.tsx"
      to: "components/ui/Footer.tsx"
      via: "import + <Footer /> after sections"
      pattern: "Footer"
---

<objective>
Build the /sandbar editorial case study page: a Server Component long-scroll `<article>` with a single `max-w-2xl mx-auto` prose column containing back navigation, the "Sandbar" h1 wordmark, the "87%" hero stat, five sections (Problem, Insight, Product, Under the Hood, What's Next), the PipelineDiagram (from plan 03-01) inside the Product section, and the Footer at the bottom.

Purpose: SAND-01 (5 sections), SAND-02 (max-w-2xl), SAND-04 (Fraunces headings / IBM Plex Mono prose), SAND-05 (Fraunces hero stat), SAND-06 (back nav). SAND-03 (the diagram) is delivered by plan 03-01 and consumed here.
Output: `app/sandbar/page.tsx` (Server Component).

This plan depends on 03-01 (PipelineDiagram must exist to import). It is Wave 2.
</objective>

<execution_context>
@C:/Users/lukec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sandbar-case-study-stubs/03-UI-SPEC.md
@.planning/phases/03-sandbar-case-study-stubs/03-RESEARCH.md

<interfaces>
<!-- Components imported by this page (all confirmed in codebase / created in plan 03-01): -->

From components/sandbar/PipelineDiagram.tsx (created in plan 03-01 — Server Component, named export):
```typescript
export function PipelineDiagram()   // renders inline SVG <figure>, takes no props
```
Import as: import { PipelineDiagram } from '@/components/sandbar/PipelineDiagram'

From components/ui/Footer.tsx (Server Component, named export):
```typescript
export function Footer()   // renders <footer> with copyable mailto link, takes no props
```
Import as: import { Footer } from '@/components/ui/Footer'

<!-- Token reference (from app/globals.css, confirmed):
     - .wordmark = Fraunces ExtraBold Italic with SOFT/WONK variation settings
     - font-mono font-medium = IBM Plex Mono 500
     - bg-[#F5F3EE] = cream (use the named alias bg-cream OR inline #F5F3EE; UI-SPEC layout uses bg-[#F5F3EE])
     - #1A1A1A = ink (default text color on cream)
     Hero stat + every h1/h2 use: className="wordmark" style={{ fontSize: 'clamp(36px, 6vw, 72px)', lineHeight: 1.1 }}
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the five case study prose sections as a content reference</name>
  <files>app/sandbar/page.tsx (prose drafted here is placed into the page in Task 2)</files>
  <read_first>
    - .planning/phases/03-sandbar-case-study-stubs/03-RESEARCH.md (lines 308-342: pipeline content + prose content guidance table + design thesis)
    - .planning/phases/03-sandbar-case-study-stubs/03-UI-SPEC.md (lines 292-310: copywriting contract — fixed strings; lines 80-86: prose typography rules)
    - .planning/STATE.md (Sandbar context: "Agentic surf forecasting", 87% forecast accuracy stat)
  </read_first>
  <action>
    Write the prose body copy for the five sections. This is a writing task — produce the actual paragraph text that Task 2 will place into the page. Keep each section to 1-2 short paragraphs (IBM Plex Mono is dense; brevity reads better). Thread the design thesis through the copy: Sandbar "translates messy, ambiguous signal into something legible and trustworthy enough to act on."

    Section content direction (write real sentences, not placeholders):
    - **Problem**: Surf forecasting is broken for people who actually surf. Generic swell forecasts report open-ocean numbers but can't account for how local bathymetry, wind, and tide interact at a specific break. The result is forecasts that are technically accurate and practically useless.
    - **Insight**: The forecast signal lives in the combination of sources, not any single one. Buoy data is ground truth, NOAA grids are context, and the ensemble logic that reconciles them is the differentiator. Good surfers already do this synthesis intuitively; Sandbar makes it systematic.
    - **Product**: Sandbar is a forecast agent that thinks like a local. It ingests live conditions and returns a spot-specific call — wave height range, quality rating, and a confidence score — reaching 87% forecast accuracy against observed conditions. (This section will contain the pipeline diagram in Task 2; reference "the pipeline below" naturally in the prose.)
    - **Under the Hood**: The agentic pipeline ingests buoy observations and NOAA grid forecasts, runs them through an ensemble model that weighs each source against historical spot behavior, then scores the output for confidence. The diagram below traces that flow.
    - **What's Next**: Real-time buoy webhooks, coverage for more breaks, and a community validation loop that lets local surfers correct the model and sharpen it over time. Sandbar is in private beta.

    Constraints:
    - Do NOT change any fixed copy from the UI-SPEC copywriting contract: h1 is exactly "Sandbar", section headings are exactly "Problem", "Insight", "Product", "Under the Hood", "What's Next", hero stat is exactly "87%" with label "FORECAST ACCURACY".
    - Prose is plain prose — no markdown bold/italic, no links inside paragraphs.
    - Use a literal em dash `—` where appropriate, not `--` or `&mdash;`.

    Deliverable of this task: the finalized paragraph strings, ready to paste into the JSX in Task 2. You may keep them inline in your working notes or stage them directly into the page file as you build Task 2 — either way, Task 2's acceptance criteria verify they land in the file.
  </action>
  <verify>
    <automated>echo "Content-drafting task — verification occurs via Task 2 acceptance criteria (prose strings present in app/sandbar/page.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five sections has at least one complete paragraph of real prose (no "TODO", no "Lorem ipsum", no placeholder text)
    - The fixed heading strings are unchanged: "Problem", "Insight", "Product", "Under the Hood", "What's Next"
    - The design-thesis idea (translating ambiguous signal into something legible/trustworthy) is reflected in at least one section's prose
    - The "87%" figure is referenced in the Product section prose
  </acceptance_criteria>
  <done>Final prose for all five sections is written and ready to place into the page; no placeholder text remains.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble app/sandbar/page.tsx with sections, diagram, hero stat, back nav, and footer</name>
  <files>app/sandbar/page.tsx</files>
  <read_first>
    - .planning/phases/03-sandbar-case-study-stubs/03-UI-SPEC.md (lines 138-165: /sandbar page layout contract; lines 210-228: back nav; lines 296-308: fixed copy)
    - .planning/phases/03-sandbar-case-study-stubs/03-RESEARCH.md (lines 364-407: back nav + section-with-aria-labelledby code examples; lines 206-244: hero stat + section heading patterns)
    - components/sandbar/PipelineDiagram.tsx (the component created in plan 03-01 — confirm the named export `PipelineDiagram` before importing)
    - components/ui/Footer.tsx (confirm named export `Footer`)
    - app/page.tsx (confirms the `<main id="main-content">` Server Component pattern and import style used in this project)
  </read_first>
  <action>
    Create `app/sandbar/page.tsx` (create the `app/sandbar/` directory). Server Component — do NOT add 'use client'. Do NOT use SceneFadeIn or any whileInView animation (the case study has no scroll animation per UI-SPEC).

    Imports:
    `import { PipelineDiagram } from '@/components/sandbar/PipelineDiagram'`
    `import { Footer } from '@/components/ui/Footer'`

    Optional (include it — low cost, adds value): a page metadata export:
    ```
    export const metadata = {
      title: 'Sandbar — Luke Cassiano',
      description: 'Agentic surf forecasting: translating buoy data and NOAA grid forecasts into actionable surf predictions with 87% accuracy.',
    }
    ```

    Export default function `SandbarPage()` returning:

    ```
    <main id="main-content">
      <article className="min-h-screen bg-[#F5F3EE] px-8 py-16 lg:px-16">
        <div className="max-w-2xl mx-auto">
          [back nav] [h1] [hero stat] [5 sections] [Footer]
        </div>
      </article>
    </main>
    ```

    Build these children of the `max-w-2xl mx-auto` div, in order:

    1. Back nav (inline at top of column):
    ```
    <a href="/" className="font-mono font-medium text-xs tracking-[0.08em] uppercase hover:opacity-60 transition-opacity duration-200 mb-16 inline-block">← Back</a>
    ```

    2. Page h1:
    ```
    <h1 className="wordmark" style={{ fontSize: 'clamp(36px, 6vw, 72px)', lineHeight: 1.1 }}>Sandbar</h1>
    ```

    3. Hero stat block:
    ```
    <div className="flex flex-col gap-1 my-8">
      <p className="wordmark" style={{ fontSize: 'clamp(36px, 6vw, 72px)', lineHeight: 1.1 }}>87%</p>
      <p className="font-mono font-medium text-xs uppercase tracking-[0.08em]">FORECAST ACCURACY</p>
    </div>
    ```
    (Note: the stat numeral is a `<p>` with .wordmark, NOT a heading — preserves heading hierarchy.)

    4. Five `<section>` elements, each with `className="py-16"` and `aria-labelledby` pointing to its own h2 id. Use these exact ids: `section-problem`, `section-insight`, `section-product`, `section-under-the-hood`, `section-whats-next`. Each section:
    ```
    <section aria-labelledby="section-problem" className="py-16">
      <h2 id="section-problem" className="wordmark" style={{ fontSize: 'clamp(36px, 6vw, 72px)', lineHeight: 1.1 }}>Problem</h2>
      <p className="font-mono font-medium text-base leading-relaxed mt-4">{prose from Task 1}</p>
    </section>
    ```
    - Use the prose paragraphs drafted in Task 1. If a section needs two paragraphs, add a second `<p className="font-mono font-medium text-base leading-relaxed mt-4">`.
    - In the **Product** section ONLY, render `<PipelineDiagram />` after the prose paragraph(s) (the diagram lives in Product per UI-SPEC line 152 / 266).
    - Headings in order: Problem, Insight, Product, Under the Hood, What's Next.

    5. After the five sections, render `<Footer />`.

    Hard constraints:
    - ALL content stays inside the `max-w-2xl mx-auto` div — no wide breakouts for the diagram or headings.
    - Exactly one `<h1>`; all section headings are `<h2>`; no skipped heading levels.
    - No blob on this page (editorial mode).
    - No 'use client', no SceneFadeIn, no whileInView.
    - `←` and `—` are literal Unicode characters, not HTML entities.
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <acceptance_criteria>
    - File `app/sandbar/page.tsx` exists
    - `grep -c "use client" app/sandbar/page.tsx` returns 0
    - `grep "import { PipelineDiagram } from '@/components/sandbar/PipelineDiagram'" app/sandbar/page.tsx` matches
    - `grep "import { Footer } from '@/components/ui/Footer'" app/sandbar/page.tsx` matches
    - `grep '<PipelineDiagram />' app/sandbar/page.tsx` matches AND `grep '<Footer />' app/sandbar/page.tsx` matches
    - `grep 'id="main-content"' app/sandbar/page.tsx` matches
    - `grep '<article' app/sandbar/page.tsx` matches AND `grep 'max-w-2xl mx-auto' app/sandbar/page.tsx` matches
    - `grep 'href="/"' app/sandbar/page.tsx` matches (back nav)
    - File contains exactly one `<h1` and exactly five `<h2` elements
    - File contains all five h2 ids: section-problem, section-insight, section-product, section-under-the-hood, section-whats-next
    - `grep -c 'aria-labelledby="section-' app/sandbar/page.tsx` returns 5
    - File contains heading text: `>Sandbar<`, `>Problem<`, `>Insight<`, `>Product<`, `>Under the Hood<`, `>What's Next<`
    - File contains `>87%<` and `FORECAST ACCURACY`
    - `grep -c 'GradientBlob\|SceneFadeIn\|whileInView\|SceneLink' app/sandbar/page.tsx` returns 0 (no blob, no scroll animation, no CTA)
    - `npx next build` completes without errors and the build output lists the `/sandbar` route
  </acceptance_criteria>
  <done>/sandbar renders the full editorial case study: back nav, Sandbar h1, 87% hero stat, five sections in order with the PipelineDiagram inside Product, and the Footer — all within a max-w-2xl column, no blob, no animation; next build passes and emits the /sandbar route.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors and lists `/sandbar`, `/belief-agent`, `/whitehelmet` routes
- /sandbar has exactly one h1, five h2 sections in order, and the PipelineDiagram inside the Product section
- All content is constrained to max-w-2xl mx-auto (no horizontal scroll, no breakouts)
- Back navigation anchor `href="/"` present at the top of the prose column
- No blob, no SceneFadeIn, no whileInView, no SceneLink on this page (editorial mode)
</verification>

<success_criteria>
- /sandbar loads all five sections in order with real prose (Problem, Insight, Product, Under the Hood, What's Next)
- Section headings + h1 in Fraunces (.wordmark); all prose/captions in IBM Plex Mono (font-mono font-medium)
- Large Fraunces "87%" hero stat with FORECAST ACCURACY label
- PipelineDiagram (from 03-01) renders inside the Product section
- Back navigation returns to the landing page
- `next build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-sandbar-case-study-stubs/03-03-SUMMARY.md`
</output>
