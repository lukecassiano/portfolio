---
phase: 02-landing-experience
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - app/page.tsx
autonomous: true
requirements: [LAND-01]
must_haves:
  truths:
    - "Visiting / renders all five scenes in order (Intro, Sandbar, Belief Agent, WhiteHelmet, Reading the Break) followed by the Footer"
    - "The five scenes stack vertically, each occupying one viewport height, with Lenis smooth scroll between them"
    - "page.tsx stays a Server Component — no 'use client' directive"
    - "The build compiles without errors"
  artifacts:
    - path: "app/page.tsx"
      provides: "Landing page assembling all five scenes + Footer"
      contains: "IntroScene"
  key_links:
    - from: "app/page.tsx"
      to: "all five scene components + Footer"
      via: "import + render in order"
      pattern: "ReadingTheBreakScene"
---

<objective>
Replace the placeholder `app/page.tsx` with the real landing page: a Server Component that imports and renders the five scene components in order (Intro → Sandbar → Belief Agent → WhiteHelmet → Reading the Break) followed by the existing Footer, inside `<main id="main-content">`. This is the only file changed in this wave — scenes are imported, not inlined. Verify the full production build.

Purpose: Final integration — turns five independent scene components into the scrolling landing experience and confirms the whole phase builds clean.
Output: Updated `app/page.tsx`; verified `npm run build`.
</objective>

<execution_context>
@C:/Users/lukec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-experience/02-UI-SPEC.md
@.planning/phases/02-landing-experience/02-RESEARCH.md

<interfaces>
<!-- Scene components from 02-02 and 02-03; Footer from Phase 1. All named exports. -->

components/scenes/IntroScene.tsx          → export function IntroScene()
components/scenes/SandbarScene.tsx        → export function SandbarScene()
components/scenes/BeliefAgentScene.tsx    → export function BeliefAgentScene()
components/scenes/WhiteHelmetScene.tsx    → export function WhiteHelmetScene()
components/scenes/ReadingTheBreakScene.tsx → export function ReadingTheBreakScene()
components/ui/Footer.tsx                   → export function Footer()  (Phase 1, mailto footer)

Current app/page.tsx is a 3-section placeholder (from 01-02) — fully replace it.
SkipLink in layout.tsx targets #main-content, which must remain on <main>.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Assemble app/page.tsx from the five scenes + Footer</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current placeholder — see exactly what is being replaced)
    - .planning/phases/02-landing-experience/02-UI-SPEC.md (Page Assembly section — exact import list and render order)
    - .planning/phases/02-landing-experience/02-RESEARCH.md (Pattern 1 — page.tsx stays a Server Component; Footer in page.tsx not layout)
    - components/ui/Footer.tsx (existing Footer export)
  </read_first>
  <action>
    Fully replace the contents of `app/page.tsx` with EXACTLY this (a Server Component — NO `'use client'` directive):

    ```tsx
    import { IntroScene } from '@/components/scenes/IntroScene'
    import { SandbarScene } from '@/components/scenes/SandbarScene'
    import { BeliefAgentScene } from '@/components/scenes/BeliefAgentScene'
    import { WhiteHelmetScene } from '@/components/scenes/WhiteHelmetScene'
    import { ReadingTheBreakScene } from '@/components/scenes/ReadingTheBreakScene'
    import { Footer } from '@/components/ui/Footer'

    export default function Home() {
      return (
        <main id="main-content">
          <IntroScene />
          <SandbarScene />
          <BeliefAgentScene />
          <WhiteHelmetScene />
          <ReadingTheBreakScene />
          <Footer />
        </main>
      )
    }
    ```

    Concrete rules:
    - NO `'use client'` at the top — page.tsx stays a Server Component (the 'use client' boundary lives in the scene/leaf components).
    - Keep `id="main-content"` on `<main>` — SkipLink in layout.tsx targets it.
    - Render order is locked: Intro, Sandbar, Belief Agent, WhiteHelmet, Reading the Break, then Footer.
    - Footer is rendered here in page.tsx, NOT in layout.tsx (locked decision).
    - Delete the entire old 3-section placeholder markup.
  </action>
  <verify>
    <automated>! grep -q "'use client'" app/page.tsx && grep -q 'id="main-content"' app/page.tsx && grep -q "<IntroScene />" app/page.tsx && grep -q "<SandbarScene />" app/page.tsx && grep -q "<BeliefAgentScene />" app/page.tsx && grep -q "<WhiteHelmetScene />" app/page.tsx && grep -q "<ReadingTheBreakScene />" app/page.tsx && grep -q "<Footer />" app/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` does NOT contain `'use client'`
    - Contains `<main id="main-content">`
    - Renders the five scenes in exact order: IntroScene, SandbarScene, BeliefAgentScene, WhiteHelmetScene, ReadingTheBreakScene
    - Renders `<Footer />` after ReadingTheBreakScene
    - No leftover placeholder text (`Scroll down — Lenis`, `Section two`, `Section three` all absent)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>app/page.tsx is a Server Component rendering all five scenes in order plus the Footer inside `<main id="main-content">`, with the placeholder content removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify production build and route output</name>
  <files>app/page.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-and-design-system/01-03-SUMMARY.md (Build Status section — expected `npm run build` output shape and that / and /_not-found prerender)
  </read_first>
  <action>
    Run the full production build and confirm it compiles cleanly with the landing page assembled. This is a verification task — no code changes unless the build surfaces an error.

    Steps:
    1. Run `npm run build`.
    2. Confirm it exits 0 with no type errors and no client/server boundary errors (a common failure mode is a Server Component importing something that uses a browser-only hook without 'use client' — but all scene components and their leaf deps already declare 'use client' where needed).
    3. Confirm the route table lists `/` as a static (prerendered) route.

    If the build fails:
    - Read the error. The most likely causes are (a) a missing 'use client' on a scene that uses motion, or (b) a typo in an import path. Fix only the offending file, then re-run `npm run build` until it exits 0.
    - Do NOT modify any locked copy, color, or contract value to make the build pass — those are correct; fix the structural/import error instead.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Build output route table includes `/` as a prerendered/static route
    - No "use client" / Server Component boundary errors in the build log
    - No TypeScript errors in the build log
  </acceptance_criteria>
  <done>`npm run build` completes with exit code 0 and the `/` route appears in the build output, confirming the assembled landing page compiles for production.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with `/` listed as a static route
- `app/page.tsx` is a Server Component (no 'use client')
- All five scenes + Footer render in the locked order inside `<main id="main-content">`
- SkipLink target `#main-content` preserved
</verification>

<success_criteria>
- `app/page.tsx` assembles the five scenes + Footer in order
- LAND-01 satisfied: five `min-h-[100svh]` scenes stacked vertically under active Lenis scroll
- Production build is green
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-experience/02-04-SUMMARY.md`
</output>
