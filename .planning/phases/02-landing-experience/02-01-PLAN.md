---
phase: 02-landing-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/SceneFadeIn.tsx
  - components/ui/ScrollHint.tsx
  - components/ui/SceneLink.tsx
autonomous: true
requirements: [LAND-07]
must_haves:
  truths:
    - "Scene content fades in on scroll entry (opacity 0→1, y 12px→0)"
    - "Fade-in is skipped when prefers-reduced-motion is set; content stays visible"
    - "Scroll hint fades to invisible after the user scrolls 80px"
    - "External CTA links open in a new tab with screen-reader 'opens in new tab' text"
  artifacts:
    - path: "components/ui/SceneFadeIn.tsx"
      provides: "Reusable whileInView fade wrapper with reduced-motion gate"
      contains: "whileInView"
    - path: "components/ui/ScrollHint.tsx"
      provides: "Scroll-driven fade hint using useScroll + useTransform"
      contains: "useTransform"
    - path: "components/ui/SceneLink.tsx"
      provides: "CTA anchor (internal + external) Server Component"
      contains: "noopener noreferrer"
  key_links:
    - from: "components/ui/SceneFadeIn.tsx"
      to: "framer-motion useReducedMotion"
      via: "conditional initial/whileInView"
      pattern: "useReducedMotion"
    - from: "components/ui/ScrollHint.tsx"
      to: "window scrollY"
      via: "useScroll() + useTransform"
      pattern: "useScroll\\(\\)"
---

<objective>
Build the three shared leaf components every scene depends on: `SceneFadeIn` (the fade-in wrapper that encapsulates LAND-07), `ScrollHint` (the intro scroll affordance), and `SceneLink` (the CTA anchor). These are pure new files with no dependencies — they unblock all five scene components in later waves.

Purpose: Centralize the animation and link contracts so scene components compose them instead of re-implementing motion logic five times.
Output: `components/ui/SceneFadeIn.tsx`, `components/ui/ScrollHint.tsx`, `components/ui/SceneLink.tsx`.
</objective>

<execution_context>
@C:/Users/lukec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-experience/02-UI-SPEC.md
@.planning/phases/02-landing-experience/02-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor must match. Use these directly — no exploration needed. -->

From components/ui/GradientBlob.tsx (already built, 'use client'):
```tsx
import { motion, useReducedMotion } from 'framer-motion'
// uses useReducedMotion() to gate animation; reduce ? static : animated
```

From lib/utils.ts:
```ts
export function cn(...inputs: ClassValue[]): string  // clsx + tailwind-merge
```

framer-motion v12.40.0 confirmed exports (import from 'framer-motion'):
motion, useScroll, useTransform, useReducedMotion. `whileInView` is a motion prop, not an import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create SceneFadeIn wrapper component</name>
  <files>components/ui/SceneFadeIn.tsx</files>
  <read_first>
    - .planning/phases/02-landing-experience/02-UI-SPEC.md (Interaction Contract → Fade-In Animations table; exact initial/whileInView/transition/viewport values)
    - .planning/phases/02-landing-experience/02-RESEARCH.md (Pattern 3 and Pitfall 8 — the both-set-when-reduced rule)
    - components/ui/GradientBlob.tsx (existing useReducedMotion gating pattern to match)
  </read_first>
  <action>
    Create `components/ui/SceneFadeIn.tsx` as a `'use client'` component (first line: `'use client'`).

    Import `{ motion, useReducedMotion } from 'framer-motion'`.

    Props interface:
    ```tsx
    interface SceneFadeInProps {
      children: React.ReactNode
      className?: string
    }
    ```

    Export a named function `SceneFadeIn({ children, className })`. Inside:
    - `const prefersReduced = useReducedMotion()`
    - Return a `motion.div` with EXACTLY these props (concrete values, do not reference tokens):
      - `className={className}`
      - `initial={prefersReduced ? { opacity: 1, y: 0 } : { opacity: 0, y: 12 }}`
      - `whileInView={prefersReduced ? undefined : { opacity: 1, y: 0 }}`
      - `viewport={{ once: true, amount: 0.3 }}`
      - `transition={{ duration: 0.6, ease: 'easeOut' }}`
    - Render `{children}` inside.

    CRITICAL (Pitfall 8): `initial` must be set to the visible state `{ opacity: 1, y: 0 }` when reduced — never leave content at opacity 0 with whileInView skipped. Both branches must be present exactly as above.

    Do NOT add per-element stagger. Do NOT wrap GradientBlob (callers keep the blob as a sibling). Export must be `export function SceneFadeIn`, not default.
  </action>
  <verify>
    <automated>grep -q "'use client'" components/ui/SceneFadeIn.tsx && grep -q "whileInView={prefersReduced ? undefined" components/ui/SceneFadeIn.tsx && grep -q "amount: 0.3" components/ui/SceneFadeIn.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `'use client'` (grep `'use client'` matches line 1)
    - Contains `import { motion, useReducedMotion } from 'framer-motion'`
    - Contains `export function SceneFadeIn`
    - Contains literal `initial={prefersReduced ? { opacity: 1, y: 0 } : { opacity: 0, y: 12 }}`
    - Contains literal `whileInView={prefersReduced ? undefined : { opacity: 1, y: 0 }}`
    - Contains `viewport={{ once: true, amount: 0.3 }}`
    - Contains `transition={{ duration: 0.6, ease: 'easeOut' }}`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SceneFadeIn renders children in a motion.div that fades opacity 0→1 and y 12→0 over 600ms easeOut on scroll entry (once, 30% visible), and renders content immediately visible when reduced-motion is set.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ScrollHint component</name>
  <files>components/ui/ScrollHint.tsx</files>
  <read_first>
    - .planning/phases/02-landing-experience/02-UI-SPEC.md (Interaction Contract → Scroll Hint section; position, copy, letter-spacing, reduced-motion render rule)
    - .planning/phases/02-landing-experience/02-RESEARCH.md (Pattern 5 and Pitfall 4 — useScroll, no useSpring, 'use client' required)
  </read_first>
  <action>
    Create `components/ui/ScrollHint.tsx` as a `'use client'` component (first line: `'use client'`).

    Import `{ motion, useScroll, useTransform, useReducedMotion } from 'framer-motion'`.

    Export a named function `ScrollHint()`. Inside:
    - `const prefersReduced = useReducedMotion()`
    - `const { scrollY } = useScroll()`  // window scroll — NO container ref, NO arguments
    - `const opacity = useTransform(scrollY, [0, 80], [1, 0])`
    - `if (prefersReduced) return null`  // conditional render, NOT animate
    - Return:
      ```tsx
      <motion.div
        aria-hidden="true"
        style={{ opacity }}
        className="absolute bottom-8 left-1/2 -translate-x-1/2 p-3"
      >
        <span className="font-mono font-medium text-xs uppercase tracking-[0.1em] text-ink">
          scroll ↓
        </span>
      </motion.div>
      ```

    CRITICAL (Pitfall: double-easing): Do NOT wrap `scrollY` in `useSpring`. Lenis already eases the scroll value. Pass the raw MotionValue from `useTransform` directly to `style={{ opacity }}`.

    The `↓` is the literal Unicode character U+2193 inside the text "scroll ↓". The `p-3` provides a 44px tap area. Export must be `export function ScrollHint`, not default.
  </action>
  <verify>
    <automated>grep -q "'use client'" components/ui/ScrollHint.tsx && grep -q "useScroll()" components/ui/ScrollHint.tsx && grep -q "useTransform(scrollY, \[0, 80\], \[1, 0\])" components/ui/ScrollHint.tsx && ! grep -q "useSpring" components/ui/ScrollHint.tsx && grep -q "if (prefersReduced) return null" components/ui/ScrollHint.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `'use client'`
    - Contains `import { motion, useScroll, useTransform, useReducedMotion } from 'framer-motion'`
    - Contains `const { scrollY } = useScroll()` (no arguments)
    - Contains `useTransform(scrollY, [0, 80], [1, 0])`
    - Does NOT contain `useSpring` (grep returns no match)
    - Contains `if (prefersReduced) return null`
    - Contains `aria-hidden="true"`
    - Contains the text `scroll ↓` and class `tracking-[0.1em]`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ScrollHint renders an aria-hidden "scroll ↓" affordance fixed at bottom-center that fades from opacity 1 to 0 as window scroll goes 0→80px, and renders nothing when reduced-motion is set.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create SceneLink CTA component</name>
  <files>components/ui/SceneLink.tsx</files>
  <read_first>
    - .planning/phases/02-landing-experience/02-UI-SPEC.md (Interaction Contract → CTA Link Style; and Accessibility Contract → External link SR text)
    - .planning/phases/02-landing-experience/02-RESEARCH.md (Pattern 6 — SceneLink is a Server Component, plain anchor)
  </read_first>
  <action>
    Create `components/ui/SceneLink.tsx` as a Server Component (NO `'use client'` directive — it is a plain anchor with no browser APIs).

    Props interface:
    ```tsx
    interface SceneLinkProps {
      href: string
      children: React.ReactNode
      external?: boolean
    }
    ```

    Export a named function `SceneLink({ href, children, external })` returning:
    ```tsx
    <a
      href={href}
      className="font-mono font-medium text-xs tracking-wide text-ink hover:opacity-60 transition-opacity duration-200"
      {...(external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
    >
      {children}
      {external && <span className="sr-only"> (opens in new tab)</span>}
    </a>
    ```

    Notes (concrete): the arrow `→` is NOT rendered here — callers include it in the `children` string (e.g. "View case study →"). No underline at rest or hover; opacity transition only. The `text-ink` class is overridden by callers on dark scenes via a wrapping className if needed — but Belief Agent has no SceneLink, so leave `text-ink` as the default. Export must be `export function SceneLink`, not default.
  </action>
  <verify>
    <automated>! grep -q "'use client'" components/ui/SceneLink.tsx && grep -q "noopener noreferrer" components/ui/SceneLink.tsx && grep -q "(opens in new tab)" components/ui/SceneLink.tsx && grep -q "hover:opacity-60" components/ui/SceneLink.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File does NOT contain `'use client'` (it is a Server Component)
    - Contains `export function SceneLink`
    - Contains `target: '_blank', rel: 'noopener noreferrer'` gated on `external`
    - Contains `<span className="sr-only"> (opens in new tab)</span>` gated on `external`
    - Contains className `font-mono font-medium text-xs tracking-wide text-ink hover:opacity-60 transition-opacity duration-200`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SceneLink renders an ink mono anchor; when external it adds target/rel attributes and a visually hidden "(opens in new tab)" span; arrow is supplied by the caller's children.</done>
</task>

</tasks>

<verification>
- All three files compile: `npx tsc --noEmit` exits 0
- `npm run build` exits 0 (no client/server boundary errors)
- SceneFadeIn and ScrollHint are `'use client'`; SceneLink is not
- ScrollHint contains no `useSpring`
</verification>

<success_criteria>
- `components/ui/SceneFadeIn.tsx`, `components/ui/ScrollHint.tsx`, `components/ui/SceneLink.tsx` all exist
- Each exports a named function matching its filename
- LAND-07 fade contract is fully encapsulated in SceneFadeIn with the reduced-motion gate
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-experience/02-01-SUMMARY.md`
</output>
